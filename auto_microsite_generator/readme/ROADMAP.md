* Optional integration hook: when `partner_microsite_manager` is installed
  and the company already carries microsite content, offer to publish its
  dynamic homepage instead of the plain welcome page (kept out of the
  automatic flow on purpose, per plan-maestro section 2.4).
* Optional theme application (e.g. the reformed `theme_corporate_multi`)
  once that module's API stabilises; theme selection is deliberately left
  out of the create hook for now.
