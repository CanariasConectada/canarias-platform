<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <!-- Server action to run validation on selected report or create new one -->
    <record id="action_server_validate_microsites" model="ir.actions.server">
        <field name="name">Ejecutar validación ahora</field>
        <field name="model_id" ref="model_microsite_validation_report"/>
        <field name="binding_model_id" ref="model_microsite_validation_report"/>
        <field name="state">code</field>
        <field name="code">
if record:
    record.action_validate_all()
        </field>
    </record>
</odoo>
