<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <!-- Template para grid de productos en búsqueda asíncrona -->
    <!-- Replica la estructura exacta de website_sale.products_grid -->
    <template id="shop_products_grid_async" name="Shop Products Grid Async">
        <!-- Este template genera SOLO el contenido interno del section#o_wsale_products_grid -->
        <t t-if="products">
            <t t-set="grid_md_allow_custom_cols" t-value="hasLeftColumn"/>
            <t t-set="grid_md_use_3col" t-value="(not hasLeftColumn and ppr == 4) or (ppr > 3)"/>
            
            <t t-foreach="bins" t-as="tr_product">
                    <t t-foreach="tr_product" t-as="td_product">
                        <t t-if="td_product">
                            <t t-set="col_height" t-value="td_product['y']"/>
                            <t t-set="col_width" t-value="12 // ppr * td_product['x']"/>
                            <t t-set="col_class" t-valuef="g-col-6"/>
                            <t t-set="col_class_lg" t-value="'g-col-lg-' + str(col_width)"/>
                            <t t-set="col_class_md" t-value="grid_md_allow_custom_cols and ('g-col-md-' + str(col_width)) or grid_md_use_3col and 'g-col-md-4' or 'g-col-md-6'"/>
                            <t t-set="col_is_stretched" t-value="(td_product['x'] >= td_product['y'] * 2)"/>
                            <t t-set="col_is_custom_portrait" t-value="not col_is_stretched and (col_height > td_product['x'])"/>
                            <t t-set="product" t-value="td_product['product']"/>
                            
                            <div
                                t-attf-class="oe_product {{col_is_custom_portrait and 'oe_product_custom_portrait'}} {{col_class}} {{col_class_md}} {{col_class_lg}} {{col_is_stretched and 'oe_product_size_stretch'}}"
                                t-attf-style="--o-wsale-products-grid-product-col-height: {{col_height}};"
                                t-att-data-colspan="td_product['x'] != 1 and td_product['x']"
                                t-att-data-rowspan="td_product['y'] != 1 and td_product['y']"
                                t-att-data-name="product_block_name"
                            >
                                <div t-attf-class="o_wsale_product_grid_wrapper position-relative h-100 o_wsale_product_grid_wrapper_#{td_product['x']}_#{td_product['y']}">
                                    <!-- Product Card Content -->
                                    <div class="oe_product_cart" data-publish="on">
                                        <div class="oe_product_image position-relative">
                                            <a t-att-href="'/shop/' + slug(product)" class="d-block h-100">
                                                <img t-if="product.image_512" 
                                                     t-att-src="'/web/image/product.template/' + str(product.id) + '/image_512'"
                                                     class="img-fluid w-100 h-100 object-fit-cover"
                                                     t-att-alt="product.name"/>
                                                <div t-else="" class="w-100 h-100 bg-light d-flex align-items-center justify-content-center">
                                                    <i class="fa fa-picture-o fa-3x text-muted"/>
                                                </div>
                                            </a>
                                            <!-- Badge de la zona si existe -->
                                            <t t-if="product.company_id and product.company_id.zone_id">
                                                <span t-attf-class="badge position-absolute top-0 end-0 m-2 {{ 'bg-info' if product.company_id.zone_id.code == 'GUA' else 'bg-success' if product.company_id.zone_id.code == 'TAM' else 'bg-warning' }}">
                                                    <t t-esc="product.company_id.zone_id.name"/>
                                                </span>
                                            </t>
                                        </div>
                                        <div class="o_wsale_product_information position-relative">
                                            <div class="o_wsale_product_information_text">
                                                <h6 class="o_wsale_products_item_title mb-2">
                                                    <a t-att-href="'/shop/' + slug(product)" class="text-decoration-none text-dark">
                                                        <t t-esc="product.name"/>
                                                    </a>
                                                </h6>
                                                <div class="product_price" itemprop="offers" itemscope="itemscope" itemtype="http://schema.org/Offer">
                                                    <span class="oe_currency_value" t-esc="product.list_price"/>
                                                    <span t-esc="website.currency_id.symbol"/>
                                                </div>
                                            </div>
                                            <div class="o_wsale_product_btn position-absolute bottom-0 end-0">
                                                <a t-att-href="'/shop/' + slug(product)" class="btn btn-primary btn-sm">
                                                    <i class="fa fa-eye"/>
                                                </a>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </t>
                    </t>
                </t>
        </t>
        <t t-else="">
            <div class="text-center py-5">
                <i class="fa fa-search fa-4x text-muted mb-3"/>
                <h4 class="text-muted">No se encontraron productos</h4>
                <p class="text-muted">Prueba con otros términos de búsqueda</p>
                <a href="/shop" class="btn btn-outline-primary">Ver todos los productos</a>
            </div>
        </t>
    </template>
</odoo>
