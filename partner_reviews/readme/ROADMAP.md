- Show the average rating badge on the public directory cards through the
  `website_directory` extension hooks (bridge module, not a hard
  dependency).
- Rate limiting / captcha for review submission if abuse appears.
- Optional email to the customer when their pending review is approved.
